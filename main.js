function power(){
	let vara= document.getElementById('a').value;
	

	window.alert(Math.pow(vara,2))
}

function round() {
        let vara = document.getElementById('a').value;
        window.alert(Math.round(vara)); 
    }

    function ran() {
        window.alert(Math.random()); 
    }

    function sqrt() {
        let vara = document.getElementById('a').value;
        window.alert(Math.sqrt(vara));
    }